<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Like">
	<select id="findLike" resultType="int">
		select count(*) from nbslike where seq = #{seq} and id = #{id}
	</select>
	
	<insert id="likeUp">
		insert into nbslike (like_no ,seq , id, likeNum)
		values((select * from (select max(like_no)+1 from nbslike) next), #{seq},#{id}, 1)
	</insert>
</mapper>